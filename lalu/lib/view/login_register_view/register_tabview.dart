import 'package:flutter/material.dart';

class RegisterTabView extends StatelessWidget {
  const RegisterTabView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text("Sign up tab");
  }
}
